{
  "name": "ai-webview-demo",
  "displayName": "AI Webview Demo",
  "publisher": "tuongroth",  
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.104.0"
  },
  "activationEvents": [
    "onCommand:aiWebview.open"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "aiWebview.open",
        "title": "AI Webview: Open"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "@types/vscode": "^1.104.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
